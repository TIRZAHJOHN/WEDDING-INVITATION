# Image Assets

Replace `/public/couple-placeholder.svg` with the real couple portrait when ready.

Recommended:
- Keep the file name as `couple-placeholder.svg`, or update `src/data/weddingData.js`.
- Use a portrait crop around `1200 x 1600`.
- Export as WebP or optimized JPEG for production.
- Keep the final file under 400 KB when possible.
